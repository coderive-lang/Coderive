name: Update source_.zip

on:
  push:
    branches:
      - main
    paths:
      - 'src/**'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 11
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '11'

      - name: Build source_.zip
        run: ./gradlew sourceZip

      - name: Commit updated source_.zip
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add source_.zip
          if git diff --cached --quiet; then
            echo "source_.zip already up to date — nothing to commit."
          else
            git commit -m "chore: update source_.zip [skip ci]"
            git push
          fi
